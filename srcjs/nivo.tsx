import { reactWidget } from 'reactR';
import { ResponsiveCalendar, ResponsiveCalendarCanvas } from '@nivo/calendar';

reactWidget(
  'calendar',
  'output',
  { ResponsiveCalendar, ResponsiveCalendarCanvas },
  {},
);
